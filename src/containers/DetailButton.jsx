import React from "react";

const DetailButton = () => {
    return (
        <button className="px-4 py-2 text-xl font-semibold text-textPrimary bg-detailBtn rounded-xl">
            Xem thêm
        </button>
    );
};

export default DetailButton;
